import LensMaterialComparison from '../LensMaterialComparison';

interface Props {
  pageId?: string;
}

export default function LensMaterialsPage(_props: Props) {
  return <LensMaterialComparison />;
}
